components:
info:
  title: Registry
  version: 3.0.0
openapi: 3.0.3
paths:
  /examples/HelloWorld:
    get:
      operationId: helloWorldPascal
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object

  /examples/helloWorld:
    get:
      operationId: helloWorldCamel
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object

  /examples/hello-world:
    get:
      operationId: helloWorldKebab
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object

  /examples/HELLO-WORLD:
    get:
      operationId: helloWorldCobol
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object

  /examples/HELLO_WORLD:
    get:
      operationId: helloWorldMacro
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object

servers:
- description: Snyk Registry
  url: /api/v3
